function positions = getXYZ(points, depthImage)
    
    numPoints = size(points, 2);
    
    for i = 1: numPoints
        
        %TODO: Calculate position in xyz
        
        
        positions(end + 1) = position;
    end
end
